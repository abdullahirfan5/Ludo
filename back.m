function [done] = back(source, eventdata)
%The callback function of the 'Exit' button on the game's main screen
%   The function returns the value of 'done' as 1 which dictates the end of
%   the game and exits.

done = 1;
exit
    
end

